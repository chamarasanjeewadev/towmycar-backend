{
  "name": "vehicle-breakdown-frontend",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "sync": "turbo repo sync"
  },
  "devDependencies": {
    "turbo": "^2.0.10"
  }
}
